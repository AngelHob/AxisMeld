# AxisMeld 待人工测试清单

更新：2026-09-12。所有项目目前均 **待人工测试**；合成事件、单元测试和截图检查不改变此状态。无需逐项反馈后才继续开发。

## 测试准备与反馈

使用 `D:/source/AxisMeld-build/phase2b-ui-test-install/blender.exe`，先保存原场景，再使用临时 Cube 场景。选择 AxisMeld Maya 2026 键位。最新安装证据以本批验收记录为准；历史批次哈希不代表当前安装。

先跑优先级 P0，再跑 P1。反馈格式：`编号 / 通过或失败 / Object或Edit / 单或四视图 / 实际结果`。涉及误触时补充鼠标路线与释放顺序；涉及显示时补充 Windows 缩放比例和视口大小。通过与失败均保留日期、版本；未反馈不推定通过。

## P0：新组件右键（M2a，安装后执行）

入口以**当前选中的活动可编辑网格**判断是否可用，模式切换沿用 Blender 原生选择集合语义，不强行改成单对象编辑。本批不做鼠标下换目标；已有活动网格时在空白处右键，仍使用当前选择上下文。鼠标下拾取与 Shift/Ctrl 建模右键后续开发。

| 编号 | 前置条件与操作 | 预期 | 人工状态 |
|---|---|---|---|
| C-01 | Object 下选 Cube；按住右键，向上/左/下拖动后松右键，分别重做 | 进入边/点/面 Edit 选择模式；一次手势仅提交一次 | 待测 |
| C-02 | 分别从点、边、面 Edit 按住右键拖向右上 Object Mode；已在 Object 再做一次 | 回 Object；已在 Object 保持 Object，不意外进入 Edit | 待测 |
| C-03 | 原地按下并松右键；拖出再回按下点释放 | 中心取消，模型与选择模式不变，无残留菜单 | 待测 |
| C-04 | 打开后按 Esc，再释放右键；再打开并切到另一应用后回来 | 取消，重新右键能打开，后续左键选择和中键操作正常 | 待测 |
| C-05 | 向右 UV、左下 Vertex Face、右下 Multi Component 释放 | 占位不可执行，不改模式/模型，不产生 Recent 或额外撤销 | 待测 |
| C-06 | Cube 活动时分别在其表面、空白处、另一个未选网格上打开 | 始终以活动 Cube 为目标；不偷偷更换或编辑鼠标下的另一个对象 | 待测 |
| C-07 | 无选中对象、活动 Camera/Light、Sculpt、其他编辑器分别右键 | 不出现本批组件热盒，原生右键仍可使用 | 待测 |
| C-08 | 有活动 Cube 时 Alt/Ctrl/Shift + 右键；另试已打开热盒后按 Ctrl 并松右键，随后无修饰右键 | 修饰键原有导航/操作不被抢占；中途修饰取消不残留已释放键，普通右键仍能打开组件热盒 | 待测 |
| C-09 | 快速连续两次右键划选；右键尚未释放时切换工具键；完成后左键选择 | 不重复提交、无粘键或释放监听残留 | 待测 |
| C-10 | 临时改绑到键盘上可用的无修饰空闲键，或禁用组件入口，重载后检查原右键，再恢复配置 | 新绑定按下打开、松该键提交；禁用/改绑不吞原生右键、不残留旧入口；个人其他配置不变；带修饰键的配置会明确拒绝 | 待测 |
| C-11 | Space → Select → Active Mesh Components 切点/边/面/Object；另从 Space → Modify → Tool Settings 打开工具环 | 与右键/QWER入口共用命令；沿用 Space 释放规则，执行后不残留鼠标监听 | 待测 |
| C-12 | 同时选两个可编辑网格，指定活动对象；用组件热盒进/出 Edit | 沿用 Blender 原生多对象编辑语义，不偷偷取消其他对象选择；切模式本身不改变几何 | 待测 |

## P0：上一批修复与工具热盒

以下以 2026-09-12 布局为准：QWER 按内容定宽；径向工具子环切换遵循当前实现，不使用早期“所有父环常驻/全部等宽”的历史验收文字。

| 编号 | 操作 | 预期 | 人工状态 |
|---|---|---|---|
| V-01 | Space 视图热盒，从左上 Left View 向左拉出按钮后释放 | 保持 Left View，不误选中左 Top View | 待测 |
| V-02 | 对 Back/Bottom 重复外侧划动；从中心做短划和原地释放 | 外侧延续对应行/列；安全区取消，无邻项误触 | 待测 |
| V-03 | 在视口四角、单视图/四视图、窄视口重复；隐藏 Camera 时划向右上空方向 | 菜单移入后仍按实际路线命中；空方向不替代执行 Left 或其他视角 | 待测 |
| T-01 | 点按 Q/W/E/R；再仅长按，不按鼠标 | 立即选工具，单纯长按不弹热盒 | 待测 |
| T-02 | 保持工具键，按住左键打开，拖向目标先松左键 | QWER 五行布局、文字和箭头不拥挤；单次正确提交 | 待测 |
| T-03 | 进入 Axis/Select 子环或设置目录，返回父级后选另一项 | 返回可预测，父子不同时抢命中，普通目录贴边且文字完整 | 待测 |
| T-04 | 分别先松左键、先松工具键、Esc、切应用；左边缘原地释放及返回起点释放 | 正确提交或取消；无意外动作或粘住状态 | 待测 |
| T-05 | W 未松时按 E；依次交换 W/E 释放顺序；长按产生键盘重复 | 新工具入口可用，旧键释放不取消新会话，不重复打开 | 待测 |
| T-06 | W/E/R 分别设不同 Global/Local/Normal；切换工具后直接拖轴和点轴后空白中键拖 | 各工具保留方向，两种拖法一致；不把已有缩放差异当成本批修复 | 待测 |
| T-07 | Q 切换 Marquee/Lasso/Paint，再松 Q；Object/Edit 执行 Clear Selection 并撤销 | 工具不被复位；Clear 只处理当前集合，一次撤销恢复；Paint 是 Blender 圆形选择适配 | 待测 |

## P1：环境与共同回归

| 编号 | 操作 | 预期 | 人工状态 |
|---|---|---|---|
| R-01 | 100%、125%、150%、200% 缩放下，单/四视图重复 C-01、V-01、T-03 | 标签可读、命中与显示一致、边缘不误选；记录实际系统缩放而非仅模拟倍率 | 待测 |
| R-02 | Space 点按切单/四视图，长按打开；移动到标准正交视图后 Alt 导航 | 点按/长按保持原规则；正交旋转锁定和导航仍正确 | 待测 |
| R-03 | UV Editor、文本输入、Sculpt 下使用 QWER/右键 | 建模入口不抢输入、不提前改变 UV 工作流 | 待测 |
| R-04 | 保留个人改键、底色、不透明度、文字颜色后重启 | 外观和绑定保留；重置外观不重置操作配置 | 待测 |
| R-05 | 两个 Blender 窗口间切换，弹窗打开时尝试热盒；返回后重试 | 不穿透弹窗、不留跨窗口按键监听；本项须实体窗口测试 | 待测 |

## 已知问题与后续范围

- **已知待修**：极窄视口 Tool Header 遮挡，不能将布局自动化通过写成此缺陷已解决。
- **M2b 待开发**：鼠标下对象拾取、完整空白上下文、Shift/Ctrl 建模组合热盒及相关操作。
- **M3 待开发**：全部建模菜单/快捷键覆盖；本清单不等于功能已齐全。
- **明确后置**：UV 全链路、Global 物体缩放算法、Vertex Face 与 Multi Component 专门能力。
- **测试设施已知限制**：旧 `AxisMeld-build/install/blender.exe` 对应的应用级 CTest 有历史失败；实际交付入口应独立验证。

## 人工结果记录

尚未收到本清单的人工结果。记录模板：`日期 | 构建哈希前12位 | 编号 | 环境 | 结果 | 复现/备注`。

# M2a final independent spec and branch review

Reviewed 2026-09-12 against baseline `12eec78b2ac`, including the final uncommitted command/adapter/catalog/profile/keymap, native session/release/parser and test changes. Read the approved M2a plan and linked modeling specification. No GUI, build, source or test edits performed by this reviewer.

## Assessment

**Ready for bounded M2a delivery; no remaining important code or specification findings.** Installation-state regression remains the coordinator's delivery gate. Manual interaction and Maya equivalence remain pending and must not be marked passed from automated evidence.

The selected, visible, editable active-mesh target matches the M2a scope. Target-under-pointer picking, complete right-click variants, Vertex Face, Multi Component and UV are explicitly deferred; they are not missing M2a implementation. `mode.object` is idempotent and appears in both Python and native whitelists. Component dispatch copies command text and cleans up before a mode change; shared source-lifetime checks precede captured region access.

## Findings closed by final re-review

- **Previous P2 fallback-test evidence defect: resolved.** The event suite now sends fresh RMB PRESS/RELEASE in unselected-mesh and non-mesh contexts and requires the actual `VIEW3D_MT_object_context_menu` draw probe to run. It no longer relies on a direct operator call using a stale RELEASE event. It also exercises real Blender user-keymap F13 remapping, disable, restored RMB, and checks native RMB remains available. Pure profile tests cover generated-data remap/disable behavior; this runtime case specifically exercises user keymap edits, not file reload.
- **Modified owned release stale guard: resolved.** `tool_modal` now clears active mouse ownership for the observed owned RELEASE and derives trigger ownership from that same event. A component release carrying Ctrl cannot create a guard waiting for a second RMB release. A QWER session still retains a genuinely held keyboard trigger after an LMB release. Read `D:/source/AxisMeld-build/m2a-modified-release-red.log`: old candidate fails on the stale-guard assertion; read latest `m2a-component-green.log`: enhanced suite passes.
- **Global keymap exception is appropriately narrow.** It matches the component command, exact Screen Editing map arguments, `screen.area_options`, no operator properties, exact unmodified RMB PRESS event and the complete default component binding. Other screen/global operators remain rejected. Verified the native operator first calls `screen_area_edge_from_cursor` and rejects a non-edge position. The native area-edge item is retained rather than deleted.
- **Select-menu capacity regression: resolved.** Removing the non-action separator offsets the new component directory, preserving eight effective entries. Python catalog assertions and native layout assertions check the directory exists and no next-page control appears.

## Release ownership and residual limits

RMB-as-trigger with mouse=0 is valid independent ownership: an old cancellation guard consumes only the old RMB release while a new W/LMB session can proceed. A new physical press of a previously owned mouse clears the old guard before passing through. Normal component release installs no residual guard. Focus-loss tests now prove cleanup and reopening without an old release. QWER and Space retain separate trigger branches.

No-active-object, region resize/invalidation and physical cross-session chord cases can remain in the manual ledger; the shared guard/lifetime code is unchanged except the observed-release correction and no new concrete defect was found. Full M2/M3 or Maya fidelity is not implied.

## Evidence reviewed or run

Reviewer-run:
- Initial component tests: 3 passed; input tests: 14 passed.
- Final component tests: 4 passed; catalog tests: 20 passed.
- Final `git diff --check`: passed (line-ending advisories only).

Read candidate modified-release RED and enhanced component GREEN logs. Coordinator reports all 57 targeted Python tests and 5 native tests passed; these were not rerun by this reviewer. Candidate/installed GUI regressions and hash verification belong to the coordinator's final acceptance record.

# M2b pointer-target final source review

Reviewed final working diff against 4d05fb4d60a on 2026-09-13. Scope: approved plan/spec, picker, native component session and dispatch, Python wrapper/capabilities, event tests and supplied build/test logs. No build, GUI or installation operations performed by reviewer.

## Verdict

No remaining actionable source-review finding in the bounded M2b scope. The prior P2 preflight gap is resolved. Installed-state validation remains owned by main; this review does not certify installed-binary consistency or manual interaction quality.

## Resolved P2: Mesh override preflight

component_target_valid in source/blender/editors/space_view3d/view3d_axismeld_hotbox.cc now rejects both Object overrides and Mesh-data overrides. The same predicate runs at capture and before selection mutation. This matches the explicit Mesh-data override rejection in native editmode_enter_ex (source/blender/editors/object/object_edit.cc:870), removing the identified predicate mismatch before commit.

Evidence correction: the initial report described a proposed linked_mesh.override_create fixture as constructible. That claim was not runtime verified by the reviewer. The implementer attempted it and reported that Mesh.override_create returned None; therefore this review does NOT claim the former defect was reproduced in a real override-Mesh fixture or that this branch has an automated runtime pass. The invalid fixture was removed from the final suite. Closure here is source-level defensive consistency, with linked/Object-override/Mesh-override scenarios explicitly retained as manual P-13, pending.

The remaining ignored dispatcher result does not by itself establish another defect requiring general rollback. No additional valid ordinary native failure scenario was established after this preflight correction. General dispatcher undo changes are outside the approved architecture and are not requested.

## Final source and test observations

- PRESS resolves a native nearest selectable Base and captures Object session UID and view-layer identity without selecting. Commit re-resolves by UID, rechecks visibility/selectability/editability and cancels an unavailable/deleted target without fallback.
- An already-selected pointer target retains the native selection set; an unselected target replaces it and becomes active. Only known enabled component commands can commit.
- Picker does not prefilter to Mesh before depth selection, so a selectable foreground non-Mesh blocks the mesh behind it. Bone IDs resolve to their Object through the native evaluator. X-Ray nearest/no-cycle adaptation is now explicit in mapping documentation.
- Edit Mesh retains its current editing set. Keyboard component invocation retains active context; Space remains on the existing dispatch path. Wrapper bypass is restricted to pointer component invocation and does not relax adapter availability globally. Placeholder leaves remain disabled.
- Source-area lifetime, modifier/owned-release handling and Q/W/E/R routing have no new modifications beyond the target capture/commit branch; no regression established in source review.
- The helper declaration is intentionally retained in ED_view3d.hh by the main owner as a generic nearest-selectable API. Existing API defaults are unchanged. This is accepted scope, not an outstanding internalization task.
- Final event source contains real deletion during a held gesture, hide_select invalidation, no-active targeting, unselected replacement, already-selected preservation, Edit non-crossing, keyboard active-other targeting and a filled foreground Curve blocker. These are assertions in the completed suite, not just comments. The invalid override fixture is absent.

## Reviewed supplied evidence

Read logs in D:/source/AxisMeld-build (did not rerun):

- m2b-build.log: blender.vcxproj produced bin/Release/blender.exe; test target build output present, no error matches found.
- m2b-python.log: 57 tests, OK.
- m2b-native.log: 5 of 5 passed.
- m2b-components.log: AXISMELD_COMPONENT_HOTBOX_EVENTS_PASS followed by Blender quit. A libpng color-profile warning is present; no suite failure is shown.

These logs support the final candidate checks reported by the implementer. They do not substitute for main's separate installed-state checks. Manual P-01 through P-13 remain pending; particularly the override fixture limitation is disclosed rather than counted as automated coverage.
